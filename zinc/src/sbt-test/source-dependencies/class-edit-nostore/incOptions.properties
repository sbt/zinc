# The storeApis=false companion fallback must fire only for classes that already had an
# object side. Editing a class with no companion object must not invalidate an unrelated
# same-name shadowing candidate (Use uses simple name B, resolved to other.B). See sbt/zinc#226.
apiDebug = false
storeApis = false
