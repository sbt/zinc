> compile

# shorten the package defined in A from 'a.b' to 'a'.
$ copy-file changes/A.scala A.scala

# 'import a.b' should now fail in B.scala
# still pending: package a.b contains no classes, so neither its existence nor its
# disappearance is visible in the analysis (contrast with import-package-removed)
-> compile
