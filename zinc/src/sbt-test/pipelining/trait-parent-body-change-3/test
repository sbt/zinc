# With pipelining, Scala 3 calls `dependencyPhaseCompleted` before it reports B's parent A, which
# it does in its Inlining phase. A change to a method body of B must not recompile C.

> root/compile
$ copy-file changes/B.scala root/B.scala
> root/compile
> root/checkRecompilations 0 A C
