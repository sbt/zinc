# The API of J comes from scalac during the compile and from J.class afterwards. The two never
# compare equal, so a changed Java source always counts as an API change.
> compile
$ copy-file changes/J.java J.java
> compile
> checkRecompilations 0 U
