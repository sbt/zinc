# Generated goldens are compared byte for byte, so keep git from normalizing their line endings
# or flagging the trailing spaces that encode Position.pointerSpace.
test-data/analysis-format-*.txt -text whitespace=-trailing-space
